using System;

class Program {
  public static void Main (string[] args) {
       DateTime HoraAtual = DateTime.Now;
      // a funçaõ DateTime.Now retorna o valor da data atual do pc
        Console.WriteLine("Data atual: " + HoraAtual);
    }
  }
